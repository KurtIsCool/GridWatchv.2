# Static resident/ops pages. Leave blank to use bundled validated data only.
GRIDWATCH_API_BASE_URL=

# Optional local CLI metadata. No Cloudflare credentials are required for mock extraction.
GRIDWATCH_ENV=development
